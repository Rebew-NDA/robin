services:
  robin:
    build: .
    container_name: robin
    restart: unless-stopped
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    expose:
      - "8501"
    networks:
      - caddy

networks:
  caddy:
    external: true
    name: caddy
